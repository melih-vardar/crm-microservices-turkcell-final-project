configurl: http://localhost:8070

spring:
  application:
    name: "userservice"
  profiles:
    active: dev
  cloud:
    config:
      uri: ${configurl}
  config:
    import: optional:configserver:${configurl}