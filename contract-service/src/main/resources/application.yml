configurl: http://localhost:8070

spring:
  profiles:
    active: local
  config:
    import: configserver:${configurl}
  application:
    name: "contractservice"
  cloud:
    config:
      profile: local
      fail-fast: true
      uri: ${configurl}


