configurl: http://localhost:8070

spring:
  application:
    name: "customerservice"
  profiles:
    active: dev
  cloud:
    config:
      profile: dev
      uri: ${configurl}
  config:
    import: optional:configserver:${configurl}